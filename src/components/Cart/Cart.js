import React from 'react';

const Cart = ({ cart }) => {
    return (
        <div>
            <p>order summery</p>
            {/* <p>selected item:{cart.length}</p> */}

        </div>
    );
};

export default Cart;